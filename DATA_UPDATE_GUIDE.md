# 暖年数据备份与更新

当前数据格式版本：`v3`。

## 推荐更新方式

1. 在应用中打开「我的 → 我的」。
2. 点击「导出数据备份」，保存生成的 `nuannian-backup-日期.json`。
3. 更新或复制应用文件后，打开新版 `index.html`。
4. 点击「导入数据更新」，选择刚才的 JSON 文件并确认。
5. 核对称呼、个人档案、收藏、打卡和用药记录是否正确。

手机端的数据与当前访问地址和浏览器绑定。更换手机、浏览器或访问地址前，请先导出 JSON；不要把“添加到主屏幕”当作数据备份。

导入会替换当前浏览器中的暖年数据，但不会修改菜谱、课程、图片或视频目录。导入前建议先导出当前数据，避免误选文件后无法恢复。

## 可更新的数据

- 个人档案：称呼、年龄、身高体重、甲状腺手术范围、眩晕史、长期腿痛、动作限制、医生/康复师备注等；
- 医生已确认的用药、补钙和低碘状态；
- 饮食忌口与目标；
- 喝水、用药、餐食、睡眠、运动打卡；
- 菜谱、课程和养护收藏；
- 每日运动前身体状态记录；
- 字号、语音提示等显示设置。

## 文件规则

- 只导入本应用导出的 JSON，或以 `data/user-data-template.json` 为基础制作的文件。
- 文件必须包含 `product: "nuannian"`；当前 `schemaVersion` 为 `3`。
- 旧版本文件会在导入时补齐缺少字段，不应手工删除 `product`、`schemaVersion` 或 `data`。
- 文件含健康资料，不要上传到公开网盘、聊天群或代码仓库。
- 单个导入文件上限 5 MB；异常 HTML 字符、过深结构和超长字段会被拒绝或清理。

## 内容库更新边界

JSON 只更新个人数据，不更新应用内置菜谱、课程、文字说明和视频来源。内容库应直接修改 `js/data-diet.js`、`js/data-exercise.js`、`js/data-fit-video.js` 等项目文件，并完成相应回归检查后再发布。
