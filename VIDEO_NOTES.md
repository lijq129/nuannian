# 烹饪视频 · 当前正式目录

版本：v0.4-inline-cooking-video。核验日期：2026-09-10。

保留 v0.3 的自然风格、此前内容基线及中文输入法修复。本次只调整烹饪视频目录、文字下方播放器与相关交互；身体动作视频继续保持关闭，不复用烹饪视频的准入规则。

## 覆盖与来源

- 68 道家常菜谱（含中式、西式面点）；三餐菜品库中 94 项需要烹饪的菜品。两类有重叠。
- 136 个明确做法关键词，对应 132 条视频。展示名别名一并收录，共 183 个精确匹配入口。不用菜名子串或搜索首条结果兜底。
- 16 项即食食材只保留处理、食用说明，不添加无关视频。
- 视频来自哔哩哔哩公开搜索页，例如 [番茄炒蛋](https://www.bilibili.com/video/BV1MfpwzcEKA/)、[刀切馒头](https://www.bilibili.com/video/BV1mp4y1B7nb/)、[蓝莓玛芬](https://www.bilibili.com/video/BV1XK411X7KL/)。每个播放器下方显示实际标题、作者与原站链接。
- 通过平台公开元数据核对标题、作者、时长、视频编号及分享/付费字段；使用 `player.bilibili.com` 官方嵌入页。不下载、不重新托管、不裁切视频，不移除平台品牌或绕过平台限制。

## 已知边界

这是烹饪参考库，不是医疗审核库。已按标题核对主要菜名、烹饪方法，并抽查实际播放；**未逐条完整观看 132 条视频，也未对作者身份与所有配方进行专业审核**。

部分基础手法示范与文字版不完全相同（例如蔬菜蒸蛋采用蒸蛋基础示范，粉丝虾仁视频用整虾，部分全麦或松饼配方不同），已在对应播放器下明确标注。用量、饮食条件和个体医嘱以文字说明及专业人员意见为准；视频中的糖、盐、油量或健康宣传不构成本应用的个体化建议。

嵌入不等于取得素材转载授权。平台可以变更外链、登录、地区和清晰度政策，作者也可以删除视频。因此不承诺永久免费、免登录、高清或在所有环境播放。原站链接始终保留，文本菜谱不依赖网络。

## 交互与设计

- 展开文字做法时才创建 iframe，初始列表不请求几十个播放器。
- `autoplay=0`、`danmaku=0`，用户决定何时播放。
- 同时最多保留一个烹饪播放器；收起、打开另一段视频或切换主页面即销毁原播放器，防止后台串音。
- 在同一页面内播放，不再要求搜索或打开弹窗。原站观看只是备用入口。
- 横屏按 16:9 展示；竖屏适当加高到 3:4 容器，保留原画面比例、不裁切，避免老年用户只能看到很小的竖屏画面。

## 核验记录

- `qa/video-metadata.json`：公开元数据核验快照；包含旧库审计结果及本轮新选择。**不是正式目录，不能将全部历史候选重新上线。**
- `qa/video-candidates.json`：本轮明确选中的 136 个关键词映射；正式发布结果是 `js/recipe-bv.js`。
- `qa/video-search*.json`：公开搜索候选快照，包含未选中的视频，不直接用于 UI。
- `qa/video-results.json`：68 个菜谱展开/收起、全部做法覆盖、即食食材、单播放器生命周期、320/390/768/1280 像素大字体布局检查通过。
- `qa/video-live-results.json`：在独立 Chrome 上从本地 `file://` 页面实际抽查番茄炒蛋、刀切馒头、蓝莓玛芬；均加载出视频，初始暂停，测试播放后时间推进，无页面脚本错误。不是全库逐条播放测试。
- `scripts/search-check.cjs`：首页及菜谱中文输入法、光标编辑、删除、粘贴和特殊字符回归通过。

## 后续维护

1. 读取本文件和正式 `js/recipe-bv.js`，明确待修改的菜品。
2. 在公开站点寻找对应教程，核对名称和做法；将选定 BVID 与必要差异备注更新到 `qa/video-candidates.json`。不要直接采纳搜索排序。
3. 运行 `node scripts/video-audit.cjs --verify` 核对元数据。该脚本缓存已成功条目；重新核验旧链接时，应只移除对应缓存项，再运行核验，不能把缓存当作实时保证。
4. `node scripts/video-catalog.cjs` 校验完整性，`node scripts/video-catalog.cjs --patch` 输出更新正式目录的补丁；审阅后使用补丁工具应用。
5. 执行 `scripts/video-check.cjs` 与 `scripts/search-check.cjs`；对变更的视频执行独立浏览器联网抽查，并更新核验日期和记录。测试需要现有 Playwright 依赖，毋须新增安装。

`scripts/video-selection.cjs` 只复现本轮从搜索快照中选择候选的过程，后续已有人工修改时不要直接运行覆盖。搜索快照、候选与核验文件均为 QA 材料；正式版本以应用加载的目录为准。
